cf set-env vault-broker VAULT_ADDR "$CF_VAULT_ADDR"
cf set-env vault-broker VAULT_TOKEN "$CF_VAULT_TOKEN"
cf set-env vault-broker SECURITY_USER_NAME "$CF_USERNAME"
cf set-env vault-broker SECURITY_USER_PASSWORD "$CF_PASSWORD"
